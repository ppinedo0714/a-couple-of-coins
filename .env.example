# Copy this file to .env and fill in values — never commit .env

# Backend API
# BACKEND_PORT=8000
# DATABASE_URL=

# Predictions service
# PREDICTIONS_PORT=8001

# Frontend (Vite env vars must be prefixed with VITE_)
# VITE_API_BASE_URL=http://localhost:8000
